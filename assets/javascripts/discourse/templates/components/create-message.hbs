{{#if canChangeType}}
  {{combo-box value=type valueAttribute='value' content=topicTypes class='type-selector'}}
{{else}}
  <span>{{typeLabel}}</span>
{{/if}}
{{#unless noCategory}}
  <span>{{i18n 'inline_composer.create.join'}}</span>
  {{category-link category}}
{{/unless}}
