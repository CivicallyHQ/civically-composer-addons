en:
  site_settings:
    compose_inline_components: "Components to insert into the inline composer"
    compose_topic_types: "Topic types to display in composer"
    compose_question_min_trust: "Min trust to create a question topic"
    compose_petition_min_trust: "Min trust to create a petition"
    compose_election_min_trust: "Min trust to create an election"
    compose_general_min_trust: "Min trust to create a general topic"
    compose_rating_min_trust: "Min trust to create a rating topic"
    compose_event_min_trust: "Min trust to create a event topic"
    compose_content_min_trust: "Min trust to create a content topic"
